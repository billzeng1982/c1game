--[[
名称：泥沼之阵
类型：军师技Buff
描述：减速50%
--]]
local ThisScriptId = 1080;

--Buff诞生处理
local function runBorn(LuaDataAgt)
	local curProgress = LuaDataAgt:GetMSProgress4Buff();	-- 获得进度
	local curAddRatio = LuaDataAgt:GetMSAddRatioPower4Buff();	-- 获得效果增益
	local maxValue1 = -0.5;
	local minValue1 = -0.1;	

	
	--计算公式
	local addValue1 = 0;
	if curProgress >= 1.0 then
		addValue1 = maxValue1;
	else
		addValue1 = minValue1 + curProgress *( maxValue1 - minValue1) * 0.6;
	end

    LuaDataAgt:ChgSpeedByRatio(1 + addValue1 * curAddRatio);
	
	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
	end
end

--Buff驱散条件判断
local function checkDeadCond(LuaDataAgt)
	
	return false;
end

--Buff驱散生效
local function runDead(LuaDataAgt)
	--属性加减回退

	local curProgress = LuaDataAgt:GetMSProgress4Buff();	-- 获得进度
	local curAddRatio = LuaDataAgt:GetMSAddRatioPower4Buff();	-- 获得效果增益
	local maxValue1 = -0.5;
	local minValue1 = -0.1;	

	
	--计算公式
	local addValue1 = 0;
	if curProgress >= 1.0 then
		addValue1 = maxValue1;
	else
		addValue1 = minValue1 + curProgress *( maxValue1 - minValue1) * 0.6;
	end

    LuaDataAgt:ChgSpeedByRatio(-(1 + addValue1 * curAddRatio));
	
	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
	end 
end

--Buff生效条件判断
local function checkEffectCond(LuaDataAgt)
	return true;
end

--Buff生效
local function takeEffect(LuaDataAgt)
	
end


Buff_1080 = {
	func_runBorn = runBorn;
	func_checkDeadCond = checkDeadCond;
	func_runDead = runDead;
	func_checkEffectCond = checkEffectCond;
	func_takeEffect = takeEffect;
}