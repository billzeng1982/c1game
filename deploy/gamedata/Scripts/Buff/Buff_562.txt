--[[
名称：觉醒鬼谋神术——伤血
类型：武将技能Buff
描述：掉血一次，该Buff加在了敌人身上
--]]
local ThisScriptId = 562;

--Buff诞生处理
local function runBorn(LuaDataAgt)
	if not IsOnServer then
		-- 播放特效（带延迟）
		local currPos = LuaDataAgt:GetCurPos();
		LuaDataAgt:PlayFX("Prefabs/BuffEffect/Buff_Gmss_Mz", LuaDataAgt, currPos, currPos, 0, false);
	end
end

--Buff驱散条件判断
local function checkDeadCond(LuaDataAgt)

	return false;
end

--Buff驱散生效
local function runDead(LuaDataAgt)
	--属性加减回退

end

--Buff生效条件判断
local function checkEffectCond(LuaDataAgt)

	return true;
end

--Buff生效
local function takeEffect(LuaDataAgt)
	local source = LuaDataAgt:GetBuffSource();
	--dancepower*10，尾数0表示是打人，尾数1表示打城墙
	local dancePowerTran = source:GetActiveSkillShareValue(0) * 10;
	LuaDataAgt:ChgHp(VALUE_TYPE.CURRENT, dancePowerTran, source, VALUE_CHG_TYPE.CHG_HP_GENERALSKILL, 2048, 0);
end

Buff_562 = {
	func_runBorn = runBorn;
	func_checkDeadCond = checkDeadCond;
	func_runDead = runDead;
	func_checkEffectCond = checkEffectCond;
	func_takeEffect = takeEffect;
}