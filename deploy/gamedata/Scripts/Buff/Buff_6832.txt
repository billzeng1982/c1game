--[[
名称：赤膊上阵
类型：被动技能
描述：增加高顺的肉搏伤害，如果华雄、董卓、高顺同时上场时，额外提升三人肉搏伤害10%<color=#53de1c>30%</color>
--]]
local ThisScriptId = 6832;


--Buff诞生处理
local function runBorn(LuaDataAgt)
	--属性加减
	
	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
	end
	
	LuaDataAgt:SetFilter(FILTER_TYPE.HP, TARGET_GROUP.NONE, 0);
end

--Buff驱散条件判断
local function checkDeadCond(LuaDataAgt)

	return false;
end

--Buff驱散生效
local function runDead(LuaDataAgt)
	--属性加减回退


	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
	end
end

--Buff生效条件判断
local function checkEffectCond(LuaDataAgt)
	return true;
end

--Buff生效
local function takeEffect(LuaDataAgt)

end

local function doFilter(LuaDataAgt, iFilterType, rFilterOwner, FilterValue)
	local IsSource = rFilterOwner:Equal(FilterValue:GetSource());
	
	if iFilterType == FILTER_TYPE.HP and FilterValue.m_iValueChgType == VALUE_CHG_TYPE.CHG_HP_ATK_NORMAL then
		local iHp = FilterValue.m_fFoo1;
		if IsSource and iHp < 0 then
			--肉搏伤害增加
			local Source = LuaDataAgt:GetBuffSource();
			local OutNormal = Source:GetPassiveSkillBaseValue(ThisScriptId, 1) / 100;
			FilterValue.m_fFoo1 = iHp * (1 + OutNormal);
			
			if not IsOnServer then
				local TarPos = LuaDataAgt:GetCurPos();
				--LuaDataAgt:Play2DFXFollowTroopInfo("Prefabs/BasicEffect/UI_CharEff_ShangHaiQiangHua", 4 , TarPos);
				LuaDataAgt:PlayFX("Prefabs/BasicEffect/3D_CharEff_ShangHaiQiangHua", LuaDataAgt, TarPos, TarPos, 4, true);
			end
		end
	end
	
	return FilterValue;
end

--这个ID要和脚本ID保持一致

Buff_6832 = {
	func_runBorn = runBorn;
	func_checkDeadCond = checkDeadCond;
	func_runDead = runDead;
	func_checkEffectCond = checkEffectCond;
	func_takeEffect = takeEffect;
	func_doFilter = doFilter;
}