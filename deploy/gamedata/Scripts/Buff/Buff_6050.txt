--[[
名称:折节好学
类型:被动技能
描述:吕蒙每次枪击成功后降低敌方物防持续2s<color=#53de1c>30%</color>并有几率沉默对方2s（buffid130001）
--]]
local ThisScriptId = 6050; 

--Buff诞生处理
local function runBorn(LuaDataAgt)
	local Source = LuaDataAgt: GetBuffSource();
	
	--属性加减
	local PassiveSkillRatio = Source:GetPassiveSkillBaseValue(ThisScriptId, 1) / 100;
	local AddStrDef = LuaDataAgt:GetStrDef(VALUE_TYPE.ORIGIN_CONF) * PassiveSkillRatio;
	LuaDataAgt:ChgStrDef(VALUE_TYPE.CURRENT, -AddStrDef);
	
	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
		
		local TarPos = LuaDataAgt:GetCurPos();
		LuaDataAgt:PlayFX("Prefabs/BasicEffect/3D_CharEff_WuFangXiaJiang", LuaDataAgt, TarPos, TarPos, 0, true); -- 延迟0播该特效
	end
end

--Buff驱散条件判断
local function checkDeadCond(LuaDataAgt)
	
	return false;
end

--Buff驱散生效
local function runDead(LuaDataAgt)
	local Source = LuaDataAgt: GetBuffSource();
	
	--属性加减
	local PassiveSkillRatio = Source:GetPassiveSkillBaseValue(ThisScriptId, 1) / 100;
	local AddStrDef = LuaDataAgt:GetStrDef(VALUE_TYPE.ORIGIN_CONF) * PassiveSkillRatio;
	LuaDataAgt:ChgStrDef(VALUE_TYPE.CURRENT, AddStrDef);
	
	if not IsOnServer then
		LuaDataAgt:RefreshFightNum();
	end
end

--Buff生效条件判断
local function checkEffectCond(LuaDataAgt)
	return true;
end

--Buff生效
local function takeEffect(LuaDataAgt)

end

Buff_6050 = {
	func_runBorn = runBorn;
	func_checkDeadCond = checkDeadCond;
	func_runDead = runDead;
	func_checkEffectCond = checkEffectCond;
	func_takeEffect = takeEffect;
}