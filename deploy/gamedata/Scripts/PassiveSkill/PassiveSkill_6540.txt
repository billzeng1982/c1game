--[[
名称:魏武忠勇
类型:被动技能
描述:夏侯惇的血量低于50%时冲击伤害增加<color=#53de1c>50%</color>
--]]
local ThisScriptId = 6540;

--技能诞生处理
local function runBorn(LuaDataAgt)
	LuaDataAgt:SetFilter(FILTER_TYPE.HP, TARGET_GROUP.NONE, 0);	
	LuaDataAgt:SetFormula(VALUE_CHG_TYPE.CHG_HP_PASSIVESKILL, ThisScriptId, 0);
	
	print("RunBorn PassiveSkill Id = "..ThisScriptId);
end

--技能条件判断
local function checkCond(LuaDataAgt)
	return true;
end

--找对象,上面条件满足后执行
local function findTarget(LuaDataAgt)
	
end

--技能生效,数值处理
local function takeEffect(LuaDataAgt)
	if IsOnServer then
		LuaDataAgt:SetSkillEnable(false);
		return;
	end
	
	LuaDataAgt:SetSkillEnable(false);
end

local function doFilter(LuaDataAgt, iFilterType, rFilterOwner, FilterValue)
	-- Filter所有者是否是发起对象
	local IsSource = rFilterOwner:Equal(FilterValue:GetSource());
	if iFilterType == FILTER_TYPE.HP and FilterValue.m_iValueChgType == VALUE_CHG_TYPE.CHG_HP_ATK_RUSH then
	    local iHp = FilterValue.m_fFoo1;
		if iHp < 0 and IsSource and not IsOnServer then
			local target = FilterValue:GetTarget();
			local maxHp = LuaDataAgt:GetHp(VALUE_TYPE.RUNTIME_CONF);
			local curHp = LuaDataAgt:GetHp(VALUE_TYPE.CURRENT);
			if curHp < 0.5 * maxHp then
				local TarPos = LuaDataAgt:GetCurPos();
				--LuaDataAgt:Play2DFXFollowTroopInfo("Prefabs/BasicEffect/UI_CharEff_EWaiShangHai", 1 , TarPos);
				LuaDataAgt:PlayFX("Prefabs/BasicEffect/3D_CharEff_EWaiShangHai", LuaDataAgt, TarPos, TarPos, 1, true);
				target:ChgHp(VALUE_TYPE.CURRENT, 0, LuaDataAgt, VALUE_CHG_TYPE.CHG_HP_PASSIVESKILL, ThisScriptId, 0);
			end
		end
	end
	
	return FilterValue;
end

--技能公式
local function doFormula(LuaDataAgt, iFormulaType, iFormulaPara, rSource, rTarget, iDamageRef)
	local extradmgRatio = LuaDataAgt:GetPassiveSkillBaseValue(ThisScriptId,1);
	local Atk = rSource:GetStrAtk(VALUE_TYPE.RUNTIME_CONF);
	iRet = Atk * extradmgRatio / 100;
	return -iRet
end

--这个ID要和脚本ID保持一致
PassiveSkill_6540 = {
	func_runBorn = runBorn;
	func_checkCond = checkCond;
	func_findTarget = findTarget;
	func_takeEffect = takeEffect;
	
	func_doFormula = doFormula;
	func_doFilter = doFilter;
}