--[[
名称:好为人师
类型:被动技能
描述:增加友军所有魏国武将的伤害，同时上场魏国武将数大于等于4个，则效果翻倍<color=#53de1c>50%</color>
--]]
local ThisScriptId = 6822; 

--技能诞生处理
local function runBorn(LuaDataAgt)
	-- 设置公式
	--LuaDataAgt:SetFormula(VALUE_CHG_TYPE.CHG_HP_PASSIVESKILL, ThisScriptId, 0);
	print("RunBorn PassiveSkill Id = "..ThisScriptId);
end

--技能条件判断
local function checkCond(LuaDataAgt)
	return true;
end

--找对象，上面条件满足后执行

local function findTarget(LuaDataAgt)

end

--技能生效，数值处理
local function takeEffect(LuaDataAgt)
	if IsOnServer then
		LuaDataAgt:SetSkillEnable(false);
		return;
	end
	
	--计算魏国武将个数
	LuaDataAgt:ClearTarget();
	local targetList = LuaDataAgt:FindTarget(TARGET_GROUP.SELF);
	
	local iCnt = 0;
	for i = 0,targetList.Count-1 do
		local target = targetList:get_Item(i);
		local check = target:GetCountryID();
		if check == 1 then
			iCnt = iCnt + 1;
		end
	end

	for i = 0,targetList.Count-1 do
		local target = targetList:get_Item(i);
		local check = target:GetCountryID();
		if check == 1 then
			if iCnt >= 4 then
				target:AddBuff(13016, LuaDataAgt, nil);
			else
				target:AddBuff(ThisScriptId, LuaDataAgt, nil);
			end
		end
	end
	
	LuaDataAgt:SetSkillEnable(false);
end

--技能公式
local function doFormula(LuaDataAgt, iFormulaType, iFormulaPara, rSource, rTarget, iDamageRef)

end


PassiveSkill_6822 = {
	func_runBorn = runBorn;
	func_checkCond = checkCond;
	func_findTarget = findTarget;
	func_takeEffect = takeEffect;
	
	func_doFilter = doFilter;
}