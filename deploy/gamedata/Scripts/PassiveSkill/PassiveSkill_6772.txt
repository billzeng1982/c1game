--[[
名称:凌统技能4
类型:被动技能
描述:增加自己的物攻<color=#f3bb25>增加自身物攻 {1} 点 </color>
--]]
local ThisScriptId = 6772;

--技能诞生处理
local function runBorn(LuaDataAgt)
		--取被动技参数
		--加物攻值
		local AddAtk = LuaDataAgt:GetPassiveSkillBaseValue(ThisScriptId,1);
		LuaDataAgt: ChgStrAtk(VALUE_TYPE.CURRENT, AddAtk);

		print("RunBorn PassiveSkill Id = "..ThisScriptId);
end

--技能条件判断
local function checkCond(LuaDataAgt)
	return true;
end

--找对象,上面条件满足后执行
local function findTarget(LuaDataAgt)

end

--技能生效,数值处理
local function takeEffect(LuaDataAgt)
	if IsOnServer then
		LuaDataAgt:SetSkillEnable(false);
		return;
	end

	-- 被动技能一场比赛只生效一次
	LuaDataAgt:SetSkillEnable(false);
end

--技能公式


--这个ID要和脚本ID保持一致
PassiveSkill_6772 = {
	func_runBorn = runBorn;
	func_checkCond = checkCond;
	func_findTarget = findTarget;
	func_takeEffect = takeEffect;
}