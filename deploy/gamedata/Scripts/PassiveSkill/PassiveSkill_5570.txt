--[[
名称：倔强不屈	
类型：被动技能
描述：加物防4%
--]]
local ThisScriptId = 5570; 

--技能诞生处理
local function runBorn(LuaDataAgt)
	  --local AddStrAtk = LuaDataAgt:GetStrAtk(VALUE_TYPE.ORIGIN_CONF) * 0.04;
	  --local AddWitAtk = LuaDataAgt:GetWitAtk(VALUE_TYPE.ORIGIN_CONF) * 0.04;		
	  local AddStrDef = LuaDataAgt:GetStrDef(VALUE_TYPE.ORIGIN_CONF) * 0.04;
	  --local AddWitDef = LuaDataAgt:GetWitDef(VALUE_TYPE.ORIGIN_CONF) * 0.04;	
	  --LuaDataAgt:ChgStrAtk(VALUE_TYPE.CURRENT,AddStrAtk);
	  --LuaDataAgt:ChgWitAtk(VALUE_TYPE.CURRENT,AddWitAtk);	
	  LuaDataAgt:ChgStrDef(VALUE_TYPE.CURRENT,AddStrDef);
	  --LuaDataAgt:ChgWitDef(VALUE_TYPE.CURRENT,AddWitDef);
end

--技能条件判断
local function checkCond(LuaDataAgt)
	return true;
end

--找对象，上面条件满足后执行
local function findTarget(LuaDataAgt)
	
end

--技能生效，数值处理
local function takeEffect(LuaDataAgt)
	if IsOnServer then
		LuaDataAgt:SetSkillEnable(false);
		return;
	end
	
	if not IsOnServer then
	    LuaDataAgt:RefreshFightNum();
	end
	
	-- 被动技能一场比赛只生效一次
	LuaDataAgt:SetSkillEnable(false);
end

--技能公式
local function doFormula(LuaDataAgt, iFormulaType, iFormulaPara, rSource, rTarget, iDamageRef)
	
end

--技能过滤
local function doFilter(LuaDataAgt, iFilterType, rFilterOwner, FilterValue)
	
end


--这个ID要和脚本ID保持一致
PassiveSkill_5570 = {
	func_runBorn = runBorn;
	func_checkCond = checkCond;
	func_findTarget = findTarget;
	func_takeEffect = takeEffect;
	
	func_doFormula = doFormula;
	func_doFilter = doFilter;
}